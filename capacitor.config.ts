import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fairoj.ionicCamera',
  appName: 'Ionic capacitor camera',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
